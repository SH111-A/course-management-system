{
  "name": "course-management-backend",
  "version": "1.0.0",
  "description": "Course Management System Backend",
  "scripts": {
    "start": "json-server database.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0"
  }
}
